Отлично, ты уточнил, что трекер привычек будет **чисто мобильным приложением** с **навигацией снизу** (bottom navigation) и **иконками вместо надписей**. Я адаптирую план, чтобы он соответствовал мобильному подходу, и разобью функционал по страницам, сохраняя чёткость и эффективность. Поскольку ты хочешь мобильное приложение, я буду ориентироваться на **React с Vite** и **Tailwind CSS** для mobile-first дизайна, а навигация будет реализована через **React Router** с иконками в нижней панели. Я также учту твой стек и подход к постепенной настройке, чтобы избежать лишней работы и сосредоточиться на ключевом функционале.

---

## 📱 Мобильное приложение: Разделение на страницы и функционал

### 🎯 Общая концепция
- **Мобильный фокус**: Приложение оптимизировано для сенсорных устройств, с большими кнопками, жестами и адаптивным дизайном.
- **Bottom Navigation**: Панель навигации внизу с иконками (без текстовых надписей).
- **Страницы**: Четыре основные страницы (Home, Stats, AddHabit, Settings) для минимального и понятного UX.
- **Пошаговый подход**: Реализуем функционал постепенно, начиная с базового, чтобы ты мог видеть прогресс и не "маяться шляпой".

### 📄 Страницы и их функционал

1. **Home (Главная страница)** 🏠
   - **Иконка**: Домик (например, `home` из библиотеки иконок).
   - **Функционал**:
     - Отображение списка привычек в виде карточек (вертикальный стек).
     - Каждая карточка содержит:
       - Название и описание привычки.
       - Цветная метка категории (например, зелёная для Здоровья).
       - Прогресс-бар (сколько дней выполнено).
       - Streak-счётчик (дни подряд).
       - Большая кнопка "Выполнено" с анимацией (подпрыгивание, конфетти).
     - Жесты: свайп влево/вправо для выполнения привычки (опционально на этапе 3).
   - **UX**: Простой вертикальный список, оптимизированный для прокрутки пальцем.

2. **Stats (Статистика)** 📊
   - **Иконка**: График или диаграмма.
   - **Функционал**:
     - Календарь активности (аналог GitHub, с зелёными квадратиками для дней выполнения).
     - Линейный график прогресса по каждой привычке (за неделю/месяц).
     - Общая статистика: активные привычки, средний процент выполнения, лучший стрик.
     - Интерактивность: при нажатии на день/график показываются детали.
   - **UX**: Компактный дизайн, чтобы графики и календарь помещались на экране телефона.

3. **AddHabit (Добавление привычки)** ➕
   - **Иконка**: Плюс.
   - **Функционал**:
     - Форма для создания привычки: поля для названия, описания, выбора категории.
     - Выбор времени напоминания (для уведомлений).
     - Кнопка "Сохранить" с анимацией.
   - **UX**: Простая форма, большие поля ввода, удобная для тапов.

4. **Settings (Настройки)** ⚙️
   - **Иконка**: Шестерёнка.
   - **Функционал**:
     - Переключение темы (светлая/тёмная).
     - Настройка уведомлений (время, частота).
     - Экспорт данных (скачать статистику в JSON).
     - Управление категориями (добавление/удаление с цветами).
   - **UX**: Чёткий список опций, переключатели и кнопки для мобильного взаимодействия.

### 🧭 Навигация
- **Bottom Navigation Bar**:
  - Расположена внизу экрана, фиксированная.
  - Содержит 4 иконки: Home, Stats, AddHabit, Settings.
  - Иконки без текста, с активным состоянием (например, подсветка или заливка).
  - Используем библиотеку иконок, например, **react-icons** (`npm install react-icons`).
  - Анимация при переключении страниц (плавное появление).

---

## 🚀 Пошаговый план реализации

### 📅 Этап 1: Базовый функционал и навигация (1 неделя)
**Цель**: Настроить мобильное приложение с навигацией и базовым функционалом привычек.

**Что реализуем**:
1. **Структура проекта**: Создаём папки (`components`, `features`, `pages`, `store`, `api`) для модульности.
2. **Bottom Navigation**: Реализуем панель навигации с иконками (react-icons).
3. **Redux Store**: Настраиваем для хранения привычек (название, описание, стрик).
4. **Home**: Отображаем статический список привычек (без бэкенда) в виде карточек.
5. **AddHabit**: Простая форма для добавления привычки (название, описание).
6. **Линтинг**: Настраиваем ESLint и Prettier для чистоты кода.

**Используемые библиотеки**:
- **React 18**: Компоненты и хуки.
- **React Router**: Навигация с bottom bar.
- **Redux Toolkit**: Хранение привычек.
- **React Hook Form**: Форма добавления.
- **Tailwind CSS**: Mobile-first стили.
- **react-icons**: Иконки для навигации.
- **ESLint, Prettier**: Чистота кода.

**Примерный результат**:
- Приложение с нижней панелью навигации (иконки: Home, Stats, AddHabit, Settings).
- На Home отображаются тестовые карточки привычек.
- На AddHabit можно добавить привычку, которая сохраняется в Redux.
- Код чистый, готов к расширению.

```javascript
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Home from './pages/Home';
import Stats from './pages/Stats';
import AddHabit from './pages/AddHabit';
import Settings from './pages/Settings';
import Layout from './components/layout/Layout';

function App() {
  return (
    <BrowserRouter>
      <Layout>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/stats" element={<Stats />} />
          <Route path="/add-habit" element={<AddHabit />} />
          <Route path="/settings" element={<Settings />} />
        </Routes>
      </Layout>
    </BrowserRouter>
  );
}

export default App;
```

```javascript
import { NavLink } from 'react-router-dom';
import { FaHome, FaChartBar, FaPlus, FaCog } from 'react-icons/fa';

function BottomNav() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 bg-blue-600 text-white flex justify-around p-2">
      <NavLink to="/" className={({ isActive }) => isActive ? 'text-yellow-300' : ''}>
        <FaHome size={24} />
      </NavLink>
      <NavLink to="/stats" className={({ isActive }) => isActive ? 'text-yellow-300' : ''}>
        <FaChartBar size={24} />
      </NavLink>
      <NavLink to="/add-habit" className={({ isActive }) => isActive ? 'text-yellow-300' : ''}>
        <FaPlus size={24} />
      </NavLink>
      <NavLink to="/settings" className={({ isActive }) => isActive ? 'text-yellow-300' : ''}>
        <FaCog size={24} />
      </NavLink>
    </nav>
  );
}

export